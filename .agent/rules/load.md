---
trigger: always_on
---

always add preloaders when there is an async activity: ie requesting from db, saving, etc.

if triggered by a button press, button should become a spinner

if triggered somewhere else, add a spinner in the middle of the screen
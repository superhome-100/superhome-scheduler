---
trigger: always_on
---

Data Refresh: Do not use realtime; instead enforce a pull-down refresh feature.

Row-Level Security: Always apply strict RLS (Row-Level Security) in the database.

Date & Time: Always use Day.js for parsing, validating, manipulating, and displaying dates and times.
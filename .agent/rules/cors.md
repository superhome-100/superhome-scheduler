---
trigger: always_on
---

check if this will be touching or implementing a supabase edge function, make sure to implement cors setup with an example at reservations-create/index.ts with the _shared lib
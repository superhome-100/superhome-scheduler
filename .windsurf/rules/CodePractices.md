---
trigger: always_on
---

File Size: Each file should not exceed 300 lines of code. If it does, split into smaller files and connect them logically.

Strict Typing: Enforce type strictness. On every migration, always regenerate types.

Models & Stores Reusability: Ensure reusable models and stores for scalability.
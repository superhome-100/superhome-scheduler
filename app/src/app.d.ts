// See https://kit.svelte.dev/docs/types#app
// SSR server locals removed; all Supabase interactions happen on the client
declare global {
  namespace App {
    // interface Locals {}
    // interface PageData {}
    // interface Error {}
    // interface Platform {}
  }
}

export {};

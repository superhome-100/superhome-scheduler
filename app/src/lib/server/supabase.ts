import type { Handle } from '@sveltejs/kit';
// This file intentionally left minimal: server client is provided via hooks.server.ts (event.locals.supabase)
// Add any shared server utilities here as needed.
export type { Handle };
